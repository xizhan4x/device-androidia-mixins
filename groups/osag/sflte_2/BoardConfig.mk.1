PREBUILT_FLS ?= hardware/intel/$(TARGET_BOARD_PLATFORM)-fls/$(TARGET_DEVICE)
MODEM_PREBUILT_FLS ?= vendor/intel/fw/modem/IMC/$(TARGET_BOARD_PLATFORM)/$(PRODUCT_MODEL)

USE_IMC_BUILD_RULES ?= true
ifeq "$(wildcard $(ANDROID_BUILD_TOP)/../soclib/)" ""
INTEL_PRG_FILE ?= $(PREBUILT_FLS)/modem_cfg_emmc.prg
GEN_PRG_FROM_SRC := true
BUILD_MODEM := 0
SOFIA_PROVDATA_FILES += $(FLSTOOL)
else
GEN_PRG_FROM_SRC := true
endif
